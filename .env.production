# =============================================================================
# Synapse Frontend — Production Environment
# =============================================================================
# This file is loaded automatically by Vite during production builds.
# It contains ONLY public URLs (no secrets).
# =============================================================================

# Backend API URL (Render)
VITE_API_URL=https://synapse-81kl.onrender.com

# WebSocket URL (auto-derived from VITE_API_URL: https → wss)
# VITE_WS_URL=wss://synapse-81kl.onrender.com
